/**
 * Mobile feature flags — flip to re-enable without deleting code.
 * Notes screens, routes, and APIs remain in the project when disabled.
 */
const ENABLE_NOTES = false;

export { ENABLE_NOTES };
