# Vite — copy to `.env` / `.env.local` (see https://vitejs.dev/guide/env-and-mode.html)

# Backend API base URL (no trailing slash issues — normalize as needed)
VITE_API_BASE_URL=http://localhost:5000/api

# Sentry — admin SPA error reporting (leave empty to disable)
VITE_SENTRY_DSN=
